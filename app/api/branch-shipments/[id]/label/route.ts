import { NextResponse } from "next/server";
import { createSupabaseServerClient } from "@/lib/supabase-server";
type BranchRelation={name?:string}|{name?:string}[]|null;type ShipmentItem={item_name:string;prepared_qty:number|string;transport_qty:number|string|null;received_qty:number|string|null};
function branchName(value:BranchRelation){if(Array.isArray(value))return value[0]?.name??"";return value?.name??""}
export async function GET(req:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;const supabase=await createSupabaseServerClient();if(!supabase)return new NextResponse("Setup error",{status:500});const {data:s}=await supabase.from("branch_chicken_shipments").select("*,branches(name,code)").eq("id",id).single();if(!s)return new NextResponse("Not found",{status:404});const {data}=await supabase.from("branch_chicken_shipment_items").select("*").eq("shipment_id",id).order("sort_order");const items=(data??[]) as ShipmentItem[];const verify=new URL(`/branch-shipments/${id}`,req.url).toString();const qr=`https://quickchart.io/qr?size=320&text=${encodeURIComponent(verify)}`;const rows=items.map((i)=>`<tr><td>${i.item_name}</td><td>${Number(i.prepared_qty).toFixed(2)}</td><td>${i.transport_qty==null?'–':Number(i.transport_qty).toFixed(2)}</td><td>${i.received_qty==null?'–':Number(i.received_qty).toFixed(2)}</td></tr>`).join('');const branch=branchName(s.branches as BranchRelation);const html=`<!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${s.shipment_no}</title><style>@page{size:101.6mm 152.4mm;margin:0}html,body{width:101.6mm;height:152.4mm;margin:0;padding:0}*{box-sizing:border-box}body{font-family:Arial,'Noto Sans Thai',sans-serif;color:#000}.label{width:101.6mm;height:152.4mm;padding:4mm 4.5mm 3.5mm;display:flex;flex-direction:column;overflow:hidden}h1{font-size:27px;line-height:1.05;margin:0 0 1.5mm;font-weight:900}.meta{font-size:16px;line-height:1.35}.number{font-size:18px;font-weight:900}.test{font-size:21px;font-weight:900;margin-bottom:1mm}table{width:100%;border-collapse:collapse;margin-top:3mm;font-size:20px;table-layout:fixed}th{font-size:17px;background:#eee;font-weight:900}th,td{border-bottom:1.5px solid #000;padding:3.2mm .7mm;text-align:right;font-weight:900;line-height:1.15}th:first-child,td:first-child{text-align:left;width:43%}th:not(:first-child),td:not(:first-child){width:19%;font-variant-numeric:tabular-nums}.bottom{margin-top:auto;display:flex;justify-content:space-between;align-items:flex-end;gap:2mm}.foot{font-size:12px;line-height:1.35;font-weight:700}.qr{width:32mm;height:32mm;display:block}.print-btn{margin-top:2mm;padding:2mm 4mm;font-size:12px;font-weight:800}@media print{.print-btn{display:none}.label{page-break-after:avoid}}</style></head><body><main class="label">${s.is_test?'<div class="test">TEST • ทดสอบระบบ</div>':''}<h1>ใบจัด/ส่งไก่หมัก</h1><div class="meta"><div class="number">${s.shipment_no}</div><div>วันที่ขาย ${s.sale_date}</div><div><b>${branch}</b></div></div><table><thead><tr><th>รายการ</th><th>จัด</th><th>ส่ง</th><th>รับ</th></tr></thead><tbody>${rows}</tbody></table><div class="bottom"><div class="foot">สถานะ: ${s.status}<br>สแกน QR เพื่อตรวจสอบใบงาน<br><button class="print-btn" onclick="print()">พิมพ์</button></div><img class="qr" src="${qr}" alt="QR"></div></main></body></html>`;return new NextResponse(html,{headers:{'content-type':'text/html; charset=utf-8','cache-control':'no-store'}})}
